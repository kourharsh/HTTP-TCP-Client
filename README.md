# HTTP-TCP-Client
Implemented my own HTTP client library using TCP Socket directly. Leverage the developed HTTP client library to develop a command-line application similar to the cURL with options such as POST, GET and verbose
